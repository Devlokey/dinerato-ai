from setuptools import setup, find_packages
setup(
    name="ury_ai",
    version="1.0.0",
    description="URY AI Restaurant Multi-Agent Operations",
    packages=find_packages(),
    install_requires=["frappe"],
)
